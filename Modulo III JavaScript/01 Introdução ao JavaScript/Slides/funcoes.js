function soma(a, b) { // acao, dentro dos parentes o atributo
    console.log(a + b); // quando voce quer ver o valor na tela, muito usado para debug
    return a +b; //quando voce precisa desse resultado
}

soma(3, 5) // como chamar a funcao, usar o nome dado anteriormente e informar os parametros